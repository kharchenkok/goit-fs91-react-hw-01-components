import styled from 'styled-components';
import { FlexList } from '../../../commonStyle/CommonStyle';

export const StatisticListStyle = styled(FlexList)(() => {
  return {
    justifyContent: 'center',
  };
});
